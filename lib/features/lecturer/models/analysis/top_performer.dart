class TopPerformer {
  const TopPerformer({
    required this.name,
    required this.id,
    required this.score,
    required this.total,
  });

  final String name;
  final String id;
  final int score;
  final int total;
}
